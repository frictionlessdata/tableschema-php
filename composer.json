{
    "name": "frictionlessdata/tableschema",
    "description": "A utility library for working with [Table Schema](https://specs.frictionlessdata.io/table-schema/)",
    "license": "MIT",
    "--require-comments--": "we should be able to support older php versions, see comments on .travis.yml",
    "require": {
        "php": ">=7",
        "justinrainbow/json-schema": "^5.2"
    },
    "require-dev": {
        "behat/behat": "^3.3",
        "phpunit/phpunit": "^6.0",
        "satooshi/php-coveralls": "^1.0"
    },
    "autoload": {
        "psr-4": {
            "frictionlessdata\\tableschema\\": "src/"
        }
    },
    "scripts": {
        "test": "behat && phpunit --debug tests/ --coverage-clover coverage-clover.xml --whitelist src/"
    }
}
